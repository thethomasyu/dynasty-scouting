/**
 * SUPERSEDED, July 2026: the long-form article renderer was replaced by the
 * visual-first system in src/components/scouting/ (VisualProfile composes
 * the modules; the canonical prose now renders through inline expansions).
 * Nothing imports this file. It exists only because the sync bridge cannot
 * delete files; cleanup-stale-names.cmd at the project root removes it.
 */
export {}
